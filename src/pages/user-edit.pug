extends ../includes/layout/layout.pug
block var
    include ../includes/_var
    - var pageName = 'Редактирование пользователя'
    - var isAut = false;
    - var isNeedSideMenu = false;
    - var activePage = 'users';
    - var className = 'users';
    - var isNeedSideMenu = true;
    - var isLogs = false;
    - var breadcrumbsList = ['Пользователи', 'Редактирование пользователя'];


block content
    +userEditPage