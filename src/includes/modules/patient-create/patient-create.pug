include data

mixin patientCreatePage
    .page__wrapper
        .page__rug
            +SectionHead({title: 'Создание пациента'})
        .page__rug
            +createPerson({list: patientCreateList})

